# Materials (locked set)

**25 materials only.** Assign these to every room / prop / pet / food mesh.

Rebuild: **`PolyPets → Materials → Rebuild Color Palette (25 mats)`**

Textures: `Assets/Art/Textures/Photo` (AmbientCG) + `Procedural` (v1 maps).  
See `docs/COLOR_PALETTE.md`.
